package build.ascribe.bridge

import mill.*
import mill.scalalib.*

object `package` extends ScalaModule with scalafmt.ScalafmtModule {

  def scalaVersion = "3.8.2"

  def moduleDeps = Seq(build.ascribe, build.ascribe.asg)
}
