package build.ascribe.bridge.test

import mill.*
import mill.scalalib.*
import mill.scalalib.TestModule

object `package` extends build.ascribe.bridge.ScalaTests with TestModule.Junit4 {

  def mvnDeps = Seq(
    mvn"org.scalameta::munit::1.0.4"
  )
}
